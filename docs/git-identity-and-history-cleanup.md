# Git Identity And History Cleanup

## 当前仓库 Git 身份

这个仓库已经设置为仅在当前项目内使用一套项目专用 Git 身份。

这个配置写在仓库本地的 `.git/config` 中，只影响本项目，不影响其他仓库的全局 Git 身份。

## 已完成的历史清理

本仓库已经完成一次 Git 历史重写，处理内容包括：

- 将历史提交作者统一为项目专用身份
- 清理历史中的 `desktop/.build` 本地产物
- 移除由本地产物带入历史的本机路径信息

已确认历史中不再包含以下信息：

- 旧作者名
- 本机用户名
- 本地绝对路径
- 旧提交邮箱
- `desktop/.build`

## 已推送状态

清理后的历史已经强制推送到远端 `origin/main`。

当前远端主分支对应的提交为：

- `751c15c42fa22e24024cea098cee8f3d9335f7ff`

## 后续注意事项

- 其他已经克隆过该仓库的副本需要重新同步历史，必要时重新 clone
- 当前仓库已经通过 `.gitignore` 忽略常见本地产物，例如 `.DS_Store`、`desktop/.build/`、`.idea/`、`.gradle/`
- 如果后续希望进一步防止误提交错误身份，可以再添加 `pre-commit` hook 做本地校验
